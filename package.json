{
  "name": "dbtv",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "import:episodes": "node scripts/import-episodes.mjs",
    "lint": "node --check scripts/*.mjs && node --check src/*.js && node scripts/lint.mjs",
    "build": "node scripts/build.mjs"
  }
}
